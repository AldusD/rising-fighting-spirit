#pragma once

#include <string>

using namespace std;

class MessagesEnum { // fake enum, to send strings
    public:
        inline static const std::string INVALID_CHOICE = "You seems to not be sure about your desires (choose valid option):\n";
};